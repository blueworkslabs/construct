<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <!-- AICore is a separate system service. This probe cannot make its own network requests. -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <application android:label="Construct Nano Lab" android:icon="@drawable/lab_icon" android:theme="@android:style/Theme.Material.NoActionBar"
        android:dataExtractionRules="@xml/backup_rules"
        android:allowBackup="false" android:fullBackupContent="false" android:usesCleartextTraffic="false" android:supportsRtl="true">
        <activity android:name=".NanoActivity" android:exported="true" android:windowSoftInputMode="adjustResize">
            <intent-filter><action android:name="android.intent.action.MAIN" /><category android:name="android.intent.category.LAUNCHER" /></intent-filter>
        </activity>
    </application>
</manifest>
