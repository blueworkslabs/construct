{
  "schemaVersion": 1,
  "id": "dev.construct.photo-identity",
  "name": "Photo identity probe",
  "version": "0.2.0",
  "description": "Synthetic acceptance fixture for stable private-photo IDs. Shows listed IDs; never treats an ID as authority.",
  "constructApi": {
    "min": "0.12.0",
    "target": "0.12.0"
  },
  "runtime": {
    "kind": "webview-js"
  },
  "entry": "ui/index.html",
  "capabilities": [
    {
      "id": "camera.photo",
      "reason": "Open the visible native viewfinder; only you can press the shutter to save a private photo."
    },
    {
      "id": "photos.library",
      "reason": "List this module’s private photos with their stable IDs and request individually confirmed deletion."
    },
    {
      "id": "image.read",
      "reason": "Required alongside the private photo library."
    }
  ]
}
